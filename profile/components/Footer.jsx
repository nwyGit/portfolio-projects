import React from 'react';
import styles from '../styles';

const Footer = () => {
	return (
		<footer className={`${styles.flexCenter}`}>
			<span>Designed & Built by Raymond Ng</span>
		</footer>
	);
};

export default Footer;
